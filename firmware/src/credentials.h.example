#pragma once

// Copia este ficheiro para credentials.h e preenche com os teus dados.
// credentials.h está no .gitignore e nunca deve ser committed.

#define WIFI_SSID       "o_teu_ssid"
#define WIFI_PASSWORD   "a_tua_password"
#define API_KEY         "bee_esp32_chave_secreta_2026"
